{
  "name": "beyond",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "start": "lerna run start",
    "prettier:write": "prettier 'packages/**/*.{ts,tsx}' --write"
  },
  "devDependencies": {
    "lerna": "^6.5.1",
    "prettier": "^2.8.4"
  },
  "workspaces": [
    "packages/*"
  ]
}
